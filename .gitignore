.DS_Store
.claude/
*.pyc
__pycache__/

# Skill outputs — never commit
outputs/

# Session state — local only
.kstack-session.json

# Local eval workspace — never commit
kstack-workspace/
evals/

# Fake/test data — never commit
fake-data/

# Experimental / unpublished skills
journey-map-coach/
